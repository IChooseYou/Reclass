build/
*.exe
*.dll
*.obj
*.o
*.a
*.lib
*.pdb
*.ilk
*.user
*.suo
.vs/
CMakeUserPresets.json
plugins/RcNetPluginCompatLayer/bridge/obj
plugins/RcNetPluginCompatLayer/bridge/bin
.cache
*.DS_Store
.claude/

# ── Out-of-tree build cruft (the real build is build/, ignored above) ──
/build2/
/build2_clean/
/CMakeCache.txt
/CMakeFiles/
/Testing/

# ── Crash dumps ──
*.dmp

# ── Scratch screenshots / captures (root-level + the two that leaked into src/).
#    Root-only & src/*.png is non-recursive, so tracked assets in docs/,
#    src/icons/, src/examples/ are untouched. ──
/*.png
/*.bmp
src/*.png

# ── Scratch test/run logs (root-level only; CMakeLists.txt etc. are not /*_… ) ──
/tmp_*
/test_*.txt
/ctest_*.txt

# ── Root-level scratch: dumps of a run, not project files ──
/Comments
/Type
/testInlineEditReEntry
/output.txt
/eprocess_*.txt
/rcxeditor_layout.txt
/scanner_sample_results.json
# A byte-identical copy of src/examples/windows-x86_64.h that lands here when
# the generator is run from the root.
/windows-x86_64.h
/*.zip

# ── Local machine config and user-only work ──
/.mcp.json
/training/
/training2/
/tools/book/

# ── Hand-made MCP request bodies and generator dumps ──
/tools/ksd_*.json
/tools/ball_*.json
/tools/test_*.json
/tools/all_25h2_structs.txt
/bench_*.txt
/ctrl_*.txt
/debug_*.txt
/*_out.txt
/*_err.txt
/*_stderr.txt
/*.out
*.log
__pycache__/
